def get_likes_count(names):
	if len(names) == null:
		return "no one likes this"
	elif len(names) == 1:
		return "Peter likes this"
	elif len(names) == 2:
		return "Jacob and Alex likes this"
	elif len(names) == 3:
		return "Max, John and Mark likes this"
	elif len(names) == 4:
		return "Max, John and two others likes this"
names = ['Peter', 'Jacob', 'Alex' ]
